#!/bin/bash

# curl https://raw.githubusercontent.com/leoheck/ubuntu-config/master/configure.sh | sudo bash -s -- -c1 -a
# curl https://raw.githubusercontent.com/leoheck/ubuntu-config/master/configure.sh | sudo bash -s -- -c2 -a
# curl https://raw.githubusercontent.com/leoheck/ubuntu-config/master/configure.sh | sudo bash -s -- -c3
# curl https://raw.githubusercontent.com/leoheck/ubuntu-config/master/configure.sh | sudo bash -s -- -c4